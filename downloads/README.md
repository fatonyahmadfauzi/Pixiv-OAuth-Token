# Downloads Folder

Folder ini disediakan untuk menyimpan artefak release manual (opsional), misalnya:

- Pixiv OAuth CLi Setup_vX.Y.Z.exe
- Pixiv OAuth CLi (Portable).exe
- Pixiv OAuth GUi Setup_vX.Y.Z.exe
- Pixiv OAuth GUi (Portable).exe

Catatan:
- File `.exe` / binary besar biasanya lebih aman dibagikan via GitHub Releases.
- Web UI menggunakan link `releases/latest/download` agar user selalu mendapat versi terbaru.
